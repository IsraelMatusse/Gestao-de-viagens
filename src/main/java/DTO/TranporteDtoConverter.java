package DTO;
/*
import org.springframework.core.convert.converter.Converter;
//import org.springframework.data.jpa.repository.query.AbstractJpaQuery.TupleConverter.TupleBackedMap;
import org.springframework.stereotype.Component;

@Component
public class TransporteDtoConverter implements Converter<TupleBackedMap, TransporteDTO> {

    @Override
    public TransporteDTO convert(TupleBackedMap source) {
        TransporteDTO dto = new TransporteDTO();
        dto.setMatricula(source.get("matricula"));
        dto.setViagensrealizadas(source.get("viagensrealizadas", String.class));
        dto.setTransporteId(source.get("transporteId", Long.class));
     
        return dto;
    }
    */


